# Binaries & build output
/build/
/dist/
*.exe
*.dll
*.so
*.dylib
jeed

# Go
*.test
*.out
vendor/
.go-version

# Local node data / keys (NEVER commit keys or live chain data)
.jeechain/
*.jeechain/
node_data/
data/
priv_validator_key.json
priv_validator_state.json
node_key.json
*.mnemonic
*.seed
keyring-test/
keyring-file/
*.address
*.info

# Browser/network captures (may contain wallet traffic, cookies, tokens)
*.har

# Debug / temp artifacts
debug-*.log
*.log
scripts/debug_*
tmp/
*.tmp
.regen-gentx/
.test-start/

# Private operator notes (IPs, seeds, deploy paths)
deploy/OPERATIONS.md

# Generated logos (keep .svg sources, ignore exported binaries if regenerated)
# assets/logo/*.png

# Secrets / environment (never commit)
.env
.env.*
*.env
.env.local
.env.*.local

# Editor / OS
.idea/
.vscode/
.cursor/
*.swp
.DS_Store
Thumbs.db
